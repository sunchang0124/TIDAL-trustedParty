import ILoginHandler from "../ILoginHandler";
import ILoginOptions from "../ILoginOptions";
import ISolidSession from "../../solidSession/ISolidSession";
import { IEnvironmentDetector } from "../../util/EnvironmentDetector";
import { ISessionCreator } from "../../solidSession/SessionCreator";
export default class PopUpLoginHandler implements ILoginHandler {
    private environmentDetector;
    private loginHandler;
    private sessionCreator;
    constructor(environmentDetector: IEnvironmentDetector, loginHandler: ILoginHandler, sessionCreator: ISessionCreator);
    canHandle(loginOptions: ILoginOptions): Promise<boolean>;
    handle(loginOptions: ILoginOptions): Promise<ISolidSession>;
}
