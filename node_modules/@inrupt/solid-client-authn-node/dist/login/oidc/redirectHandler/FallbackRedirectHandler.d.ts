import { IRedirectHandler, ISessionInfo } from "@inrupt/solid-client-authn-core";
export declare class FallbackRedirectHandler implements IRedirectHandler {
    canHandle(redirectUrl: string): Promise<boolean>;
    handle(_redirectUrl: string): Promise<ISessionInfo & {
        fetch: typeof fetch;
    }>;
}
