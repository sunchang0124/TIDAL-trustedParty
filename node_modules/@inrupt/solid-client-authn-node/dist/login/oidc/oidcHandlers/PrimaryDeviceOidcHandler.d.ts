import { IOidcHandler, IOidcOptions, LoginResult } from "@inrupt/solid-client-authn-core";
export default class PrimaryDeviceOidcHandler implements IOidcHandler {
    canHandle(_oidcLoginOptions: IOidcOptions): Promise<boolean>;
    handle(_oidcLoginOptions: IOidcOptions): Promise<LoginResult>;
}
