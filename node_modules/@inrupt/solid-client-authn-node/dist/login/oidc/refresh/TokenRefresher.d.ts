import { IClientRegistrar, IIssuerConfigFetcher, IStorageUtility } from "@inrupt/solid-client-authn-core";
import { TokenSet } from "openid-client";
import { JWK } from "jose";
export interface ITokenRefresher {
    refresh(localUserId: string, refreshToken?: string, dpopKey?: JWK.ECKey): Promise<TokenSet & {
        access_token: string;
    }>;
}
export default class TokenRefresher implements ITokenRefresher {
    private storageUtility;
    private issuerConfigFetcher;
    private clientRegistrar;
    constructor(storageUtility: IStorageUtility, issuerConfigFetcher: IIssuerConfigFetcher, clientRegistrar: IClientRegistrar);
    refresh(sessionId: string, refreshToken?: string, dpopKey?: JWK.ECKey): Promise<TokenSet & {
        access_token: string;
    }>;
}
