import { JWK } from "jose";
import { fetch } from "cross-fetch";
import { ITokenRefresher } from "../login/oidc/refresh/TokenRefresher";
export declare type RefreshOptions = {
    sessionId: string;
    refreshToken: string;
    tokenRefresher: ITokenRefresher;
};
export declare function buildBearerFetch(accessToken: string, refreshOptions?: RefreshOptions): typeof fetch;
export declare type DpopHeaderPayload = {
    htu: string;
    htm: string;
    jti: string;
};
export declare function normalizeHttpUriClaim(audience: string): string;
export declare function createDpopHeader(audience: string, method: string, key: JWK.ECKey): string;
export declare function buildDpopFetch(accessToken: string, dpopKey: JWK.ECKey, refreshOptions?: RefreshOptions): Promise<typeof fetch>;
