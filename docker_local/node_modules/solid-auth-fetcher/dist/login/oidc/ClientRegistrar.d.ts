import { IFetcher } from "../../util/Fetcher";
import IClient from "./IClient";
import ILoginOptions from "../ILoginOptions";
import IIssuerConfig from "./IIssuerConfig";
export interface IClientRegistrar {
    getClient(options: ILoginOptions, issuerConfig: IIssuerConfig): Promise<IClient>;
}
export default class ClientRegistrar implements IClientRegistrar {
    private fetcher;
    constructor(fetcher: IFetcher);
    private performDynamicRegistration;
    getClient(options: ILoginOptions, issuerConfig: IIssuerConfig): Promise<IClient>;
}
