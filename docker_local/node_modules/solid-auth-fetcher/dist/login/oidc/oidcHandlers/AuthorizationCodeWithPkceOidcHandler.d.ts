import IOidcHandler from "../IOidcHandler";
import IOidcOptions from "../IOidcOptions";
import ISolidSession from "../../../solidSession/ISolidSession";
import { ISessionCreator } from "../../../solidSession/SessionCreator";
import IJoseUtility from "../../../jose/IJoseUtility";
import { IStorageUtility } from "../../../localStorage/StorageUtility";
import { IRedirector } from "../Redirector";
export default class AuthorizationCodeWithPkceOidcHandler implements IOidcHandler {
    private sessionCreator;
    private joseUtility;
    private storageUtility;
    private redirector;
    constructor(sessionCreator: ISessionCreator, joseUtility: IJoseUtility, storageUtility: IStorageUtility, redirector: IRedirector);
    canHandle(oidcLoginOptions: IOidcOptions): Promise<boolean>;
    handle(oidcLoginOptions: IOidcOptions): Promise<ISolidSession>;
}
