import IOidcHandler from "../IOidcHandler";
import IOidcOptions from "../IOidcOptions";
import { IFetcher } from "../../../util/Fetcher";
import { IDpopHeaderCreator } from "../../../dpop/DpopHeaderCreator";
import ISolidSession from "../../../solidSession/ISolidSession";
import { ISessionCreator } from "../../../solidSession/SessionCreator";
export default class LegacyImplicitFlowOidcHandler implements IOidcHandler {
    private fetcher;
    private dpopHeaderCreator;
    private sessionCreator;
    constructor(fetcher: IFetcher, dpopHeaderCreator: IDpopHeaderCreator, sessionCreator: ISessionCreator);
    canHandle(oidcLoginOptions: IOidcOptions): Promise<boolean>;
    handle(oidcLoginOptions: IOidcOptions): Promise<ISolidSession>;
}
