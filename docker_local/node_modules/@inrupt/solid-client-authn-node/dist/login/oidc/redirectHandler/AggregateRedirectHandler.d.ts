import { IRedirectHandler, ISessionInfo, AggregateHandler } from "@inrupt/solid-client-authn-core";
export default class AggregateRedirectHandler extends AggregateHandler<[string], ISessionInfo & {
    fetch: typeof fetch;
}> implements IRedirectHandler {
    constructor(redirectHandlers: IRedirectHandler[]);
}
