import { IClientRegistrar, IIssuerConfigFetcher, IRedirectHandler, ISessionInfo, ISessionInfoManager, IStorageUtility } from "@inrupt/solid-client-authn-core";
import { IdTokenClaims } from "openid-client";
import { ITokenRefresher } from "../refresh/TokenRefresher";
export declare function getWebidFromTokenPayload(idToken: IdTokenClaims): Promise<string>;
export declare class AuthCodeRedirectHandler implements IRedirectHandler {
    private storageUtility;
    private sessionInfoManager;
    private issuerConfigFetcher;
    private clientRegistrar;
    private tokenRefresher;
    constructor(storageUtility: IStorageUtility, sessionInfoManager: ISessionInfoManager, issuerConfigFetcher: IIssuerConfigFetcher, clientRegistrar: IClientRegistrar, tokenRefresher: ITokenRefresher);
    canHandle(redirectUrl: string): Promise<boolean>;
    handle(inputRedirectUrl: string): Promise<ISessionInfo & {
        fetch: typeof fetch;
    }>;
}
